# Official Documentation Topology

This directory is reserved for static, theoretical, academic, and official project documentation.

Files following the `Aequitas-MAS_*.md` convention must live here instead of the repository root.

Dynamic AI state, plans, and operational artifacts must remain confined to `.ai/` and `.context/`.
