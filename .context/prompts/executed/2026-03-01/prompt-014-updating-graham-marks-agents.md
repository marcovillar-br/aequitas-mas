@workspace
As my AI Developer (GCA), update the model name in `src/agents/graham.py` and `src/agents/marks.py` to maintain consistency across the system.

Task Instructions:
1. Open `src/agents/graham.py` and `src/agents/marks.py`.
2. Locate the initialization of the `ChatGoogleGenerativeAI` model in both files.
3. Change the `model` parameter to `"gemini-2.5-flash"`.
4. Ensure `temperature=0.0` for Graham and `temperature=0.2` for Marks are preserved.
5. All code, variables, and comments must remain in English.