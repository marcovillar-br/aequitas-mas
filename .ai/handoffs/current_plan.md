---
plan_id: plan-doc-integrity-fix-001
target_files:
  - "CLAUDE.md"
  - ".ai/adr/001-ssot-and-lazy-persistence-loading.md"
  - ".context/rules/coding-guidelines.md"
enforced_dogmas: [artifact-driven-communication, strict-formatting, scope-discipline, ssot]
validation_scale: "FACTS (Mean: 5.0)"
---

## 1. Intent & Scope

Apply the 3 Recommended Actions from `DOC_AUDIT_REPORT.md` (generated by
`aidd-004` integrity audit, 2026-03-24). This plan targets exactly the
findings catalogued in that report and nothing else.

- **Action 1 (HIGH):** Remove the two stale `.context/skills/` references
  from `CLAUDE.md` (lines 9 and 11). This is a breaking issue: every task
  session loads `CLAUDE.md` first and currently receives routing instructions
  for a non-existent directory.
- **Action 2 (MEDIUM):** Inject a historical deprecation notice into
  `ADR 001` to warn readers that `.ai/context.md` is archived and the
  current SSOT is `.ai/aidd-001-unified-system-prompt.md`.
- **Action 3 (ADVISORY):** Inject an SSOT cross-reference note at the top
  of `coding-guidelines.md` to clarify the relationship between the
  normative rule file and the canonical dogma definitions in `aidd-001`.

**SCOPE GUARD:** ARTIFACT-ONLY. Zero modifications to `.py`, `.tf`, `.yml`,
or `.sh` files. Zero modifications to `src/`, `tests/`, `infra/`, `scripts/`.
The Implementer MUST perform FULL FILE REWRITES for all three targets to
prevent formatting corruption.

---

## 2. File Implementation

### Step 2.1 — FULL FILE REWRITE: `CLAUDE.md`

* **Action:** Rewrite `CLAUDE.md` applying exactly two text substitutions.
  All other lines must be preserved byte-for-byte.

* **Substitution A — Line 9:**
  Remove the `.context/skills/` reference from the routing instruction.

  BEFORE (exact current text):
  ```
  3. **`.context/agents/skills-index.md`** — Central routing map for specialized skills. Use it to decide when a task requires additional context from `.context/skills/` or `.ai/skills/`.
  ```

  AFTER (exact replacement text):
  ```
  3. **`.context/agents/skills-index.md`** — Central routing map for specialized skills. Use it to decide when a task requires additional context from `.ai/skills/`.
  ```

* **Substitution B — Line 11:**
  Remove the `.context/skills/*.md` frontmatter reference.

  BEFORE (exact current text):
  ```
  Treat the YAML frontmatter in `.context/skills/*.md` and `.ai/skills/*/SKILL.md` as the canonical metadata source for skill routing. Keep the visible body sections aligned with that metadata.
  ```

  AFTER (exact replacement text):
  ```
  Treat the YAML frontmatter in `.ai/skills/*/SKILL.md` as the canonical metadata source for skill routing. Keep the visible body sections aligned with that metadata.
  ```

* **Constraints:** Only lines 9 and 11 change. Every other line in the file
  must be preserved exactly, including all whitespace, blank lines, and the
  `---` section separators.

---

### Step 2.2 — FULL FILE REWRITE: `.ai/adr/001-ssot-and-lazy-persistence-loading.md`

* **Action:** Inject the deprecation notice block between the closing `---`
  of the YAML frontmatter (line 8) and the `# ADR 001:` heading (line 10).
  Insert a blank line before and after the blockquote to maintain standard
  Markdown spacing.

  The injected block (insert after line 8, before line 10):
  ```markdown

  > **⚠ SUPERSEDED REFERENCE:** This ADR was written when `.ai/context.md`
  > served as the SSOT for all architectural dogmas. That file has since been
  > archived to `.ai/archive/context.md`. The current canonical SSOT is
  > `.ai/aidd-001-unified-system-prompt.md`. The lazy-loading decision
  > documented in Section 2.2 remains valid and is unaffected by this change.

  ```

* **Constraints:** No other content in the ADR body may be altered. The
  YAML frontmatter block (lines 1–8), all section headings, and the full
  body text of Sections 1, 2, and 3 must be preserved exactly.

---

### Step 2.3 — FULL FILE REWRITE: `.context/rules/coding-guidelines.md`

* **Action:** Inject the Dogma Source cross-reference note immediately after
  the existing opening blockquote (line 4), before the `# Constraints` heading
  (line 6). Insert a blank line between the existing blockquote and the new
  one for visual separation.

  The injected block (insert after line 4, before line 6):
  ```markdown

  > **Dogma Source:** The canonical definitions of all non-negotiable dogmas
  > (Risk Confinement, Controlled Degradation, Temporal Invariance, Inversion
  > of Control) live in `.ai/aidd-001-unified-system-prompt.md`. This file
  > focuses on the practical implementation rules and coding patterns derived
  > from those dogmas.

  ```

* **Constraints:** The existing opening blockquote on lines 3–4 must remain
  intact. All section headings, rule bodies, naming conventions, and the
  External Dependencies section must be preserved exactly. Only the new
  blockquote is added.

---

## 3. Definition of Done (DoD)

- [x] `CLAUDE.md` line 9: no longer contains `.context/skills/` — verified
  by reading the file after rewrite.
- [x] `CLAUDE.md` line 11: no longer contains `.context/skills/*.md` —
  verified by reading the file after rewrite.
- [x] `.ai/adr/001-ssot-and-lazy-persistence-loading.md` contains the
  `> ⚠ SUPERSEDED REFERENCE:` block between frontmatter and `# ADR 001:`.
- [x] `.context/rules/coding-guidelines.md` contains the
  `> **Dogma Source:**` block after the opening system instruction blockquote.
- [x] `grep -n "context/skills" CLAUDE.md` returns zero matches.
- [x] All three files pass a read-back check confirming the rest of their
  content is unchanged.
- [x] **HARD CONSTRAINT:** Zero modifications to `.py`, `.tf`, `.yml`,
  or `.sh` files.
- [x] **HARD CONSTRAINT:** No changes to `src/`, `tests/`, `infra/`,
  `scripts/`, or `.ai/archive/`.
